# =============================================================================
# .dockerignore — Server
# Excludes files that don't belong in the Docker build context.
# =============================================================================
node_modules
dist
.env
.env.local
.env.production
*.log
.git
.gitignore
README.md
